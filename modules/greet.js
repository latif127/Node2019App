var greet={
    hi: (name)=>{
        console.log(`Hi: ${name}`);
    },
    hello: (name)=>{
        console.log(`Hello: ${name}`);
    },
    gm:(name)=>{
        console.log(`Good Morning: ${name}`);
    }
};

module.exports=greet;